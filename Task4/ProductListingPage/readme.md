Develop a Product Listing Page with Filtering and Sorting Options :

Build an interactive page where:

Products are displayed dynamically.

Users can filter by category.

Users can sort by price (low → high / high → low).

The UI updates instantly without page reload.